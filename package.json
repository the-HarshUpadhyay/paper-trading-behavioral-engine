{
  "name": "nevup-track1-backend",
  "version": "1.0.0",
  "description": "NevUp Track 1 — System of Record Backend for Trade Journal & Behavioral Analytics",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/migrate.js",
    "seed": "node src/seed.js",
    "worker": "node src/workers/index.js",
    "generate-token": "node scripts/generate-token.js",
    "test": "node --test tests/**/*.test.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "ioredis": "^5.4.2",
    "pg": "^8.13.1",
    "pino": "^9.6.0",
    "pino-http": "^10.4.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
